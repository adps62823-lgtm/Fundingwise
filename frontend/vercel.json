{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/splash", "dest": "/splash.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
